'Set the offset height to the Z prxoimity switch after Manually Zeroing Z
' ... useful for Handibots and Desktops (or other tools) with Z proximity switches
'  Note that using the Z proximity in this way requires marking that your tool has 
' ... a Z prox switch in the checkbox in Tools > Shopbot Setup [TS]


If &ATC = 0 Then GoTo IsMTC

IsMTC:
	FP,C:\SbParts\SetZprox.sbp

END